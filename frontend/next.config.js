/** @type {import('next').NextConfig} */
const nextConfig = {
  // Vercel handles Next.js natively — no need for static export.
  // This enables SSR/ISR for better LCP performance.
}

module.exports = nextConfig
